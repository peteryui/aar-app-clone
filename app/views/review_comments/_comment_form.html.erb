<%= simple_form_for [@review, Comment.new  ] do |f| %>

  <%= f.input :comment, :label => "Comments ( support Markdown syntax) ", :input_html => { :rows => 10, :data => { :provide => "markdown"} } %>


  <!-- Single button -->
  <div class="btn-group pull-right">

    <button type="button" class="btn btn-default" onclick="$('#search_type').val('store'); $('#new_comment').trigger('submit.rails');" > Submit </button>
    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> <span class="caret"></span> <span class="sr-only">Toggle Dropdown</span> </button>


    <ul class="dropdown-menu">
      <li onclick="$('#search_type').val('store'); $('#new_comment').trigger('submit.rails');" >
        <a href="#" >Send as open ( TODO )</a>
      </li>
      <li onclick="$('#search_type').val('store'); $('#new_comment').trigger('submit.rails');">
        <a a href="#">Send as closed  ( TODO )</a>
      </li>
    </ul>
  </div>

<% end %>
