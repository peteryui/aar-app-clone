<div class="row">
<div class="col-sm-9 col-sm-offset-1">

  <div class="clearafix">


    <div class="action pull-right" style="margin-right:20px;">

      <%= ibutton("New Todo", new_review_todo_path(@review) , :remote => true, :class => "btn btn-default", :iclass => "fa fa-plus") %>


        <!-- Split button -->
        <div class="btn-group  btn-group">
          <button type="button" class="btn btn btn-default">修改選項</button>
          <button type="button" class="btn btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <span class="caret"></span>
            <span class="sr-only">Toggle Dropdown</span>
          </button>
          <ul class="dropdown-menu pull-right">
            <li>  <%= ibutton("編輯", edit_event_review_path(@review.event_id, @review), :remote => true, :iclass => "fa fa-pencil") %>
            </li>

            <li>
              <%= ibutton("刪除", event_review_path(@review.event_id, @review), :method => :delete, :iclass => "glyphicon glyphicon-trash", :data => { :confirm => "Are you sure?"}) %>

            </li>
          </ul>
        </div>
    </div>

  </div>
  <h2> <%= @review.subject %> </h2>

<hr>
<h3> 為什麼沒有達到預期的效果？實際上發生什麼事？差別在哪裡？ </h3>

<%= @review.should_happen.to_markdown %>

<h3>哪些方法有效？哪些無效？為什麼</h3>
<%= @review.actual_happen.to_markdown %>

<h3>下次應該怎麼做</h3>

<%= @review.improvement_plan.to_markdown %>


  <% if @todos.present? %>

    <%= render :partial => "review_todos/todo_list" %>

    <% end %>

    <% if @comments.present? %>
      <%= render :partial => "review_comments/comment_list" %>
    <% end %>

    <hr>
    <%= render :partial => "review_comments/comment_form" %>

  </div>
</div>
